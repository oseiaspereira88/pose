dist-release/
*.test
pose-mcp/pose-mcp
.pose/bin/
.idea/
.qwen/

# Gerado por tests/release/compat.sh (Compatibility gate) — sem isso, o
# arquivo fica untracked em CI e goreleaser recusa rodar com "git is in a
# dirty state" (achado real, primeira execução completa do pipeline).
compatibility-report.md

# Gerados pelo passo de assinatura da extensão de referência em release.yml,
# antes do goreleaser — mesmo motivo acima: o v0.20.0 falhou exatamente assim
# (spec pose-extension-reference-publication).
pose-rule-kubernetes.tar.gz
pose-rule-kubernetes.sigstore.json

# Cache local de sincronização do agente (mapa arquivo→SHA-256). Não é estado
# do projeto: foi versionado por engano em acff2e5 e o mapa disparou o
# generic-api-key do gitleaks, derrubando o Security gate em todas as releases
# de v1.7.1 a v1.7.10 (spec pose-release-security-gate-integrity).
.harne8-agent-sync.json
