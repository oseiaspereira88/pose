{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "crisol://pose-mcp/schemas/v1/gate-result.json",
  "title": "POSE Gate Result",
  "description": "Outcome of a deterministic POSE gate (pose_check, pose_lint_spec) evaluated read-only: the exit code is the verdict, the output is the evidence. A failing gate is a result, not a tool error.",
  "type": "object",
  "properties": {
    "command": { "type": "string" },
    "exit_code": { "type": "integer" },
    "passed": { "type": "boolean" },
    "output": { "type": "string" }
  },
  "required": ["command", "exit_code", "passed", "output"],
  "additionalProperties": true
}
