# POSE v0.16.4

## Fixed

- `pose install` e `pose upgrade` agora preservam edições customizadas de maquinários através de backups `.pose-backup`, e os templates scaffold embutidos foram sincronizados com as ferramentas nativas de assessment, state e invocações do binário `pose`. (pose-scaffold-backup-preservation)

