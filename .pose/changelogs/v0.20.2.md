# POSE v0.20.2

## Fixed

- The independent release verification no longer aborts before it checks anything. The consumer-side extension check read `$dir`, a variable that script does not define — `$assets` is its name there — so the whole verification failed under `set -u`. v0.20.1 published correctly but could not be verified as a result. (pose-verifier-assets-variable-fix)

