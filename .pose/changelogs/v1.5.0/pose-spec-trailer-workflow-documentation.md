---
spec: pose-spec-trailer-workflow-documentation
category: changed
breaking: false
refs: ISSUE#29
---

Documented the mandatory `POSE-Spec: <slug>` Git commit trailer requirement across `AGENTS.md`, `POSE.md`, task workflows (`feature`, `bugfix`, `refactor`), AI agent skills (`pose-feature`, `pose-bugfix`, `pose-spec-closeout`), and distributed engine scaffolds in English and Portuguese (`pt-BR`).
