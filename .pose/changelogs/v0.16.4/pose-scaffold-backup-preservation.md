---
spec: pose-scaffold-backup-preservation
category: fixed
breaking: false
refs:
---

`pose install` e `pose upgrade` agora preservam edições customizadas de maquinários através de backups `.pose-backup`, e os templates scaffold embutidos foram sincronizados com as ferramentas nativas de assessment, state e invocações do binário `pose`.
