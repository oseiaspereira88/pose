# POSE v1.8.1

## Fixed

- `pose doctor`'s `review.evidence-vocabulary` check now inspects only the review profiles that `.pose/policy/review.json` selects. It previously reported every profile on disk, so a project that owns its profiles and leaves the shipped ones in place was warned about files that build no plan. (pose-doctor-selected-profiles-only)

