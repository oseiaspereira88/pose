{
  "_comment": "Staleness policy for the native project-state artifact (spec pose-project-state-artifact). Optional: absent means pose-mcp uses these same defaults. max_age_days is checked against generated_at; max_commits is checked against baseline_commit via `git rev-list --count`, when a git repo is available.",
  "max_age_days": 7,
  "max_commits": 20
}
