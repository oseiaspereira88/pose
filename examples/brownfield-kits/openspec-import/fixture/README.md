# Auth Service

A pre-existing service already using OpenSpec for one in-flight change
(`openspec/changes/add-notifications/`). This README and `src/auth.py`
predate POSE and must read identically after import.
