# Widget Service

A small pre-existing service with no spec system of any kind — the
starting point for the direct-adoption kit. This README predates POSE and
must read identically after adoption.

## Running

```
go run ./service
```
