# Notify Service

A pre-existing service already using GitHub Spec Kit for one in-flight
feature (`.specify/specs/001-user-notifications/`). This README and
`src/notify.py` predate POSE and must read identically after import.
